if(DEFINED ENV{OE_CMAKE_TOOLCHAIN_FILE})
  include($ENV{OE_CMAKE_TOOLCHAIN_FILE})
endif()

set(CMAKE_SYSTEM_PROCESSOR aarch64)
if(DEFINED ENV{CROSS_COMPILE_PREFIX})
  set(CMAKE_C_COMPILER $ENV{CROSS_COMPILE_PREFIX}gcc)
  set(CMAKE_CXX_COMPILER $ENV{CROSS_COMPILE_PREFIX}g++)
elseif(DEFINED ENV{CC} AND ENV{CXX})
  set(CMAKE_C_COMPILER $ENV{CC})
  set(CMAKE_CXX_COMPILER $ENV{CXX})
endif()

if("$ENV{VLINK_HOST_PLATFORM}" STREQUAL "linux-aarch64")
  set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM BOTH)
  set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY BOTH)
  set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE BOTH)
  set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE BOTH)
else()
  set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM NEVER)
  set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY ONLY)
  set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE ONLY)
  set(CMAKE_FIND_ROOT_PATH_MODE_PACKAGE ONLY)
endif()

include(${CMAKE_CURRENT_LIST_DIR}/linux.toolchain.common.cmake)
